import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VerfiymobilecodePage } from './verfiymobilecode.page';

describe('VerfiymobilecodePage', () => {
  let component: VerfiymobilecodePage;
  let fixture: ComponentFixture<VerfiymobilecodePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VerfiymobilecodePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
