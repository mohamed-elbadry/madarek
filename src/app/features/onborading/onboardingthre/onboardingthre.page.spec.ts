import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OnboardingthrePage } from './onboardingthre.page';

describe('OnboardingthrePage', () => {
  let component: OnboardingthrePage;
  let fixture: ComponentFixture<OnboardingthrePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OnboardingthrePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
